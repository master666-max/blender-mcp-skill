# CHANGELOG — blender-mcp skill

> 版本语义：patch = 修错不改行为；minor = 增量条款/工具；**major = 行为契约变更（立版级，如强制格式首次设立）**。
> 详细的逐版发现与闭环记录见 `evals/regression-checks.md` 台账与各轮验收报告
> （审计侧留档 `_verify_*/`，不入包）。

## v2.9.3（2026-09-25）

**自审整改轮（F-167/F-168/F-169）**（patch——作者侧自审 3 低危，全部当日闭合；**交付契约零变更**）：
- 背景：用户令「你自己审计」——按审计方纪律在**发行 zip 纯净解包副本**上复验四查 + 被测件
  逐字节比对 + **历版 7 个 zip 引擎哈希链逐一复算**（全部与台账相符）+ 工具对抗用例。
  四查/锚定位力/常数三件全绿；揪出 3 低危（报告归档 research-hub 本线
  `研究/自审报告_v2.9.2_20260925.md`）：
- **F-167**：`exp_hint.py` 命中计数与 `--top` 切片耦合——`--top 0` 实测报「命中 0/14·无命中
  照实说」而同查询真命中 2 条，**有命中被宣告无命中**（违反工具自身纪律）→ 改为先算
  **全量命中数**再显示（超量注明「显示前 k 条」）+ `--top <1` 显式 ERROR exit 2 拒绝静默钳制；
- **F-168**：`--engine` 无关键词时旗标被静默吞；且 SKILL §0 步5/§3 行/门面命令块的
  `scripts/exp_hint.py` 均为相对路径、CWD=包根的假设三处未注明 → 加提示行 + 三处补
  「在 skill 包根执行」；
- **F-169**：`exp_hint.py` 过引擎质量门被拒（`subprocess 不在白名单`——`--engine` 的正当
  依赖），与桥门拒同族但边界未载 → engine/README 边界节补载；U-09 行补 P3 门检记录
  （门拒不改——宿主件按设计不适用引擎白名单）；
- **勘误**：v2.9.2 条目所称「台账 U-08 行补注」实落 **U-09 行尾**（该换装注记追加位置记错）——特此更正；
- 台账 F 行（宽式）**138 → 141**（+F-167/168/169，EXPECTED_LEDGER_ROWS 同步=登记动作）；
  条款 **95**/探针 **11**/指纹 **95** 不变；引擎件零变更（v0.2.2 保持）。

## v2.9.2（2026-09-25）

**演化引擎换装：上游 v0.2.2（§7 投影语义对齐 + 账本事务化）**（patch——vendored 依赖更新，
CLI/宿主契约零变更；**来源迁址**：上游根级单文件已迁入打包目录 `evo-seat/`，架构书随迁
`evo-seat/docs/架构书-微内核-20260923.md`，engine/README 来源行同步）：
- `evo_seat.py` 更新为 **v0.2.2**：① **§7 投影语义对齐**（与重装形态 evocore.project 同语义——
  建条盖账本 ts / 命中回放 last_used / 墓碑生效）；② **账本事务化并入机制段**（`append`
  取尾→INSERT 包进 `BEGIN IMMEDIATE`/COMMIT/ROLLBACK + `busy_timeout=5000`——七阶段
  审计流水线 R1/R2 修复进原版；**并发冒烟**：4 写者×20 条各自独立连接，0 错误、80 事件、
  链验通过——两写者同读尾哈希致链分叉的老风险被事务串行化关掉）；
- **换装证据**：selftest 11 测全过 + 质量门自食 + **conformance 14/14** + 新库/旧账本
  **双向兼容**（0.2.1 建的 28 事件账本 0.2.2 直开链完整）+ 桥 14 EXP 入账链完整；
- **新基线：`framework_sha = 622110a5af2cc4d7`（框架段 15,875 B，独立复算一致）**；
- **变更记录失准如实入档（同期发现，已回报上游）**：上游 0.2.2 变更记录称「机制段 §1-§6
  未动 ⇒ framework_sha 不变」——实测 §1-§6 **+499 B**（事务化并入）、哈希已变。漏记被
  段哈希对账**当场检出**——0.2.1 判别力修复的直接收益（旧实现下此漏记不可见）；
- **指纹 #92 第四次按登记动作换锚**（`096077ce…` → `512f13b86…`）；台账 U-08 行补注；
- **三处残留守卫洞复测仍在**（内嵌判别力测试不调用本体 / selftest 目录发现 / 失败退出码
  仍 0）——继续挂账待上游；指纹 **95**、探针 **11**、条款 **95**、台账 F 行 **138** 均不变。

## v2.9.1（2026-09-23）

**exp_hint.py 增引擎检索通路（`--engine`）**（patch——工具增强，零规则/零契约变更）：
- 承接审计区提示「`evo_seat.py retrieve` 是现货、比文件版多三件」——**逐项实测**：
  ① **命中留痕**：`cmd_retrieve` 每次命中即 `append("engine","memory_retrieve_hit",
  {entry_id,query})`（实测：一次检索 2 命中 → 账本 **28 → 30 事件**，两条留痕 payload 可查）；
  ② **last_used 刷新**：G1+ 命中即 `touch()`（账面事实，供审计）；
  ③ **类型衰减排序**：`DECAY={episodic:2.0, semantic:1.0, procedural:0.0}`——**口径如实：
  本区条目全部为 procedural（零衰减），各条衰减乘数同为 0，故"类型衰减排序"在本区
  无区分度**；engine 通路在本区的真实增益 = 命中留痕 + 引擎分词打分（实测
  「烘焙 方向」→ EXP-014 10.67 / EXP-011 6.17，与文件扫排序一致）；
- 落地：`scripts/exp_hint.py --engine <关键词>` 代跑引擎检索并按行贴出结果，后附文件扫对照；
  **账本未初始化时明示并自动回退文件扫**——缺省通路**永不依赖引擎**（账本=运行态，不入包、
  不入 git、不入部署，换机/未初始化都能用）；检测到账本而未加 `--engine` 时提示一行；
- **P3 自跑三通路（全过）**：缺省文件扫 / `--engine` 无账本→回退 / `--engine` 有账本→
  引擎输出+留痕入账（`audit` 复算 30 事件）；
- 指纹 **95** 不变（#95 锚工具首行未动）；探针 **11** 不变（`--repeat 2` = 24/24）；
  台账 F 行 138 不变；引擎件零变更（v0.2.1 保持）。

## v2.9.0（2026-09-23）

**U-09 经验区运行时提醒（用户令 A+B）**，版本取 **minor**（新增能力：一条文本纪律 + 一件小工具）：
- **A（文本挂点）**：`SKILL.md` **§0 连接自检**增第 5 步「**开工前扫经验区**（每次任务必做）」
  ——跑 `scripts/exp_hint.py <任务关键词>`，**命中条目先读"规避法"再动手**；§3 任务路由表
  置顶一行同义指路；12 分册 §7 增**读取侧**指路（原「新增教训请先落」为写入侧，两侧对称）；
- **B（机读工具）**：新增 `scripts/exp_hint.py`——零依赖、只读 `experience/` 下 EXP 条目
  （**不依赖引擎账本**：账本是运行态、不入包，换机/未初始化也可用）；关键词加权命中
  （id/一句话/claims×3、规避法/现象×2、其余×1）；**无命中照实说"无命中"、不虚构相关条目**；
- **P3 自跑（新工具先拿自己跑一遍，四组全过）**：空参列 14 条 /「烘焙 方向」→EXP-014 首位 /
  「父化 矩阵」→EXP-010 首位 / 冷僻词→「命中 0/14」（照实报）；
- **设计取舍（报备）**：提醒链刻意落在**包内文本 + 一条命令**，**不依赖引擎**——引擎做后台
  账本与治理，不做上下文注入；宿主钩子（自动注入）因包无法强制宿主行为，未采纳（如需可另议）；
- 指纹 92 → **95**（#93 SKILL §0 提醒入口 / #94 12 分册读取侧 / #95 工具在位用法自锚）；
  **探针 10 → 11**（+P-N93：删 §0 开工前步 → FAIL #93）；条款 92 → **95**；
  台账 F 行（宽式）**138** 不变（U-09 不计）；**引擎件零变更**（v0.2.1 保持）。

## v2.8.3（2026-09-23）

**演化引擎换装：上游 v0.2.1（framework_sha 判别力修复版）**（patch——vendored 依赖更新，
CLI/宿主契约零变更；上游为**修复本包 v2.8.2 轮实测报出的缺陷**）：
- `evo_seat.py` 更新为审计区 **v0.2.1**：`framework_sha()` 原实现取 `src.find("§1 core")`
  首个命中（落在文件头 docstring 区段名清单上，只哈希 66 字节、对框架码偏离零判别力）——
  现改为**横幅行正则定位**（`^# ═+ §1 core` → `^# ═+ §7 cli`）+ **<5000B fail-closed 抛错**；
  内嵌 `test_framework_sha_discriminates`（selftest 10 → 11 测）；审计区 **conformance §F
  从「字段在位」升级为判别力测试**（破坏副本对跑，两侧同值即 FAIL）；
- **回执核对（本包独立实测，四项声明全部属实）**：新基线 `framework_sha = a98f559753d7cbe0`
  + 段长 **13,297 B** 与回执逐字吻合且独立复算一致；**改动-对照哈希必变**
  （`a5f09e30…` ≠ `a98f5597…`，判别力真修）；真件 conformance **14/14 PASS** vs
  回退件（撤销修复）**13/14 FAIL**（exit 1）——「检查检查器」属实；
- **同期实测三处残留守卫洞（已回报上游，待 v0.2.2）**：① 内嵌判别力测试为**本地复算**、
  不调用 `framework_sha()` 本体（隔离目录回退件 11 测仍 OK——真守卫是 conformance §F）；
  ② `selftest` 用目录发现（`pattern="evo_seat.py"`），**重命名副本会静默测到邻文件**
  （实测：污染目录里回退件跑出「9 测 OK」，实为另一旧文件）；③ `selftest` 失败时
  **退出码仍 0**（fail-closed 违背，脚本化判定须读输出文本）；
- 换装证据：selftest **11 测**全过 + 质量门自食通过 + **旧账本向下兼容复验**（28 事件链
  完整，`framework_sha=a98f559753d7cbe0`）+ 桥重跑 14 EXP 链完整（G3）；
- **指纹 #92 第三次按登记动作换锚**（`403d79a4…` → `096077ce…`）；台账 U-08 行补注；
- 对账口径（采纳上游回执 §四建议）：**段哈希为主**（区分「框架升级」与「宿主段改动」）
  + 整文件 sha256 为**投递校验**（engine/README 首部登记值 + 指纹 #92）；
- 指纹 **92** 与条款 **92** 不变（换锚不加条）；台账 F 行（宽式）**138** 不变；探针集不变。

## v2.8.2（2026-09-23）

**演化引擎换装：上游版本纪律版 v0.2.0**（patch——vendored 依赖更新，CLI/宿主契约零变更；
上游 v0.2.0 的破坏性语义在「**老算法库拒读**」，本包账本为 v2 算法、无重建面）：
- `evo_seat.py` 更新为审计区 **v0.2.0**：`VERSION`/`HASH_ALGO` 常量与**版本变更记录**入档
  （0.1.0=v1 哈希只覆盖 payload；0.2.0=**v2 哈希把 `actor`/`kind` 并入输入**——老算法库
  open 拒绝并要求按事件流重建）；SCHEMA 增 `meta.hash_algo` 键；缺键的在途 v2 库按
  「首行试算」自动识别补键放行（不误杀不静默）；新增内嵌断言
  `test_version_and_algo_declared`；
- 换装证据：引擎 selftest **10 测**全过 + 质量门自食通过 + **conformance --fused 13/13** +
  **旧账本向下兼容复验**（本包 28 事件链完整，`framework_sha=89adf037485b4148`）+
  桥重跑 14 EXP 入账链完整（G3 档）；
- **指纹 #92 第二次按登记动作换锚**（`43f8db8f…` → `403d79a4…`）——U-08 预置的升级
  语义再实跑一次；台账 U-08 行补注；
- **同期实测披露（上游边界，本包不修——vendor 逐字节原则）**：`framework_sha()` 的切片是
  `src.find("§1 core")` 的**首个命中**——落在文件头 docstring 的区段名清单上（实测 66 字节），
  **框架代码改动不改变该值**（改动-对照实测：破坏 §2 store 内代码后输出不变）。SPEC §F 的
  T-E 副本对账判别力近零，与我方 F-155 恒真锚同族；本包以**整文件 sha256 兜底**（README
  首部登记值 + 指纹 #92 锚定逐字节）并在 engine/README 载明边界注，已报出待上游处置；
- 指纹 **92** 与条款 **92** 不变（换锚不加条）；台账 F 行（宽式）**138** 不变；
  探针集不变——**11** ＝ 10 破坏探针 + 正例对照（脚本输出口径：
  `10 探针 × 2 轮 + 正例对照 = 22/22`，`--repeat 2`；两口径同源同值，此处注明防歧义）。

## v2.8.1（2026-09-23）

**演化引擎换装：上游 SPEC 对齐版**（patch——vendored 依赖更新，CLI/契约零变更）：
- `evo_seat.py` 更新为审计区 SPEC 对齐版：**SPEC 符合性声明**（SPEC-内核接口与宿主
  契约-v1）+ **治理位边界**（验收/裁决/修宪不入内核——执行无权自宣验收，治理位
  永远外置）+ **`framework_sha()` 副本对账**（§1core…§6gates 文本段哈希，verify/
  audit 顺带输出——合并形态副本与权威版对账，T-E 防御）；audit 增 [符合]/[边界] 行；
  level 输出区分升档/降级措辞；
- 换装证据：引擎 selftest 9 测全过 + 质量门自食通过 + **旧账本向下兼容复验**
  （既往 28 事件链完整，`framework_sha=89adf037485b4148`）+ 桥重跑 14 EXP 入账链完整；
- **指纹 #92 按登记动作换锚**（`61a808be…` → `43f8db8f…`）——U-08 预置的升级语义
  首次实跑；台账 U-08 行补注；
- 指纹 **92** 不变（换锚不加条）；条款 92；台账 F 行（宽式）**138** 不变。

## v2.8.0（2026-09-23）

**U-08 evo-seat 微型内核嵌入经验区**（用户令"开始嵌入"；架构书《evo-seat 微型内核
v1》+ 上游 `evo_seat.py` v0.1.0 均由审计区产出），版本取 **minor**（新增演化引擎能力）：
- **experience/engine/** 落地：`evo_seat.py`（单文件治理×演化融合，**vendored 逐字节
  未改**，sha256 `61a808be…`）+ `README.md`（来源/哈希/六档说明/字段映射表/双路径/
  边界声明/不变量逐字承架构书）+ `exp_bridge.py`（EXP ↔ 引擎桥：payload 映射、
  幂等投影、G3 默认档）；
- **冒烟证据（新工具先拿自己跑）**：引擎 selftest 9 测全过；**质量门自食通过**
  （`gate evo_seat.py`）；桥 `import` 14 EXP → 28 事件、链完整、检索命中
  （"烘焙 方向"→EXP-014、"父化 矩阵"→EXP-010）；
- **runner EXP 检查扩展**：引擎三件在位断言（缺失即 EXP-ZONE CHECK FAIL）；
- 指纹 **#92**：引擎 vendored 哈希锚——**升级引擎须同步替换该哈希**（登记动作语义，
  与双登记常数同族）；
- **make_zip 排除 `experience/state`**（引擎账本=运行态，不入发行包/不入 git）；
- 边界：桥不过引擎质量门（import 白名单是引擎自包含契约；桥=宿主侧适配器——
  README 已载明，引擎自食门以其自身为准）；
- **嵌入不放宽任何不变量**（append-only/出处三件套/fail-closed/墓碑不删/决策留痕/
  构造校验/内嵌测试）；引擎写权只 `draft`，晋升过人 + 发版门；
- 指纹 91 → **92**；条款 91 → **92**；台账 F 行（宽式）**138** 不变（U-08 不计）；
  探针 **11** 不变。

## v2.7.0（2026-09-23）

**U-07 经验区落地（用户令"开干"，设计稿 `_经验区设计拟稿_v1.md` 治理主张全数采纳）**，
版本取 **minor**（新增经验区能力与 runner 检查项）：
- **新增 experience/ 目录**（随包发行）：INDEX.md 账本（只增不改 + 快照时点标注 +
  引擎接口声明）+ **EXP-001~014**（迁移自 12 §7 教训登记表 BMCP-ERR-001~014，
  status=promoted、legacy 字段记映射、证据三件套=登记表原话+grep 复算）；
- **runner 增 EXP 区结构校验**（新退出码 **4**）：frontmatter 必填键/证据三件套/
  状态合法值/promoted 必有 promotion/INDEX 覆盖——**活区管结构不管数量**（不做条目数
  常数、不做内容指纹）；
- **治理主张**：经验区追加不触发发版（活区）；**晋升进分册才是规则变更**（走四查 +
  配指纹）；演化引擎（self-evolving-kb v1.0.0）只写 `draft`，晋升过人 + 发版门
  （免疫通道）；
- 12 §7 表尾经验区指路（指纹 **#90**）；INDEX 活区声明（指纹 **#91**）；
  探针 **P-NEXP**（状态值破坏 → EXP-ZONE CHECK FAIL exit 4）；
- **B3 基线归档落地**：新增 `evals/baselines/`，发版时归档四查 + M13 输出（借鉴
  EDD 基线对比）；
- **指纹 89 → 91**；探针 10 → **11**（M13 11/11；--repeat 2 = 22/22 见发布记录）；
  台账 F 行（宽式）**138** 不变（U-07 行不计入）；条款 **91**；
- **稳态重排**：v2.6.0 候选对照未及验收即被本能力轮叠加（F-153 同族显式闭合），
  候选对照顺延为 **(v2.7.0, v2.7.1)**。

## v2.6.0（2026-09-21）

**外部借鉴落地轮 + 欠账兑现**（F-165/F-166 + U-06 借鉴 6 项），版本取 **minor**
（新增发行签名能力与探针统计能力；交付契约零变更、零工件变更）：
- **F-165**（中危·虚报兑现）：①查 定义句改并列两项——条款集 `EXPECTED_CLAUSES`
  （违反 exit 2）与台账集 `EXPECTED_LEDGER_ROWS`（违反 exit 3）——F-159/F-161；
  编辑后 grep 回读验证（F-165 教训的执行形态）；
- **F-166**（低危）：台账集断言加 ASCII 标签 **`LEDGER-SET CHECK FAIL`**（与条款集
  `CLAUSE-SET CHECK FAIL` 同形），P-NL161 改判该标签——恢复与断言一一对应的特异性；
- **B1 发行签名**（借鉴 NVIDIA Verified Skills / 供应链研究）：发行 zip 附 SSH 签名
  （ed25519，minisign 式），`assets/release-signing/` 内含公钥与 allowed_signers，
  INSTALL 增验签节 + 指纹 **#87**；私钥不在本仓；
- **B2 探针统计**（借鉴 tau-bench pass^k）：`run-negative-probes.py` 增 `--repeat N`，
  按探针聚合触发率 k/N；
- **B4/B5/B6/B7 文档四件**：14 分册目验判据验证器规范（对照 arXiv 2026-04 Verifiers
  论文，指纹 **#89**）；00 分册敷衍④补 Stechly《Self-Verification Limitations》引用；
  11 分册增「行业安全基线对照与威胁案例」附录（DoD/CSA/CoSAI/arXiv 普查 + ClawHub
  341 恶意技能等四案例，指纹 **#88**）；
- **B8/B9/B10**（SDD 命令化入口 / 验收过程分栏 / 3D-Agent 生态跟踪）登记于
  research-hub 本线借鉴登记件，稳态后解锁；
- **指纹 86 → 89**（#87 验签命令 / #88 安全附录 / #89 验证器规范）；探针 9 → **10**
  （+P-N87，M13 自检 9/9 → 10/10，支持 --repeat 聚合）；台账 F 行（宽式）136 → **139**
  （F-165/F-166/U-06）；
- **版本语义裁定**：B1 签名与 B2 统计为新增能力 → minor（非 v2.5.10 修复号）；
  候选稳态宣告对照相应顺延为 **(v2.6.0, v2.6.1)**（方案 A：规则/能力变更轮只要
  〔F-153 同族显式闭合（v2.7.0 注）：本对照未及验收即被 U-07
  能力轮（经验区）叠加——现行候选对照见 v2.7.0 条目〕
  零新增发现即可计入第一拍）；
> payload 说明：相对最近一次受审载荷（v2.5.9 @ 66ea5edc），本版 diff = ①公开脱敏
> 6 文件（v2.5.9 公开重建 @ 1ea2ea5b，见该条目脱敏注记）+ ②本条目全部变更。
> 脱敏与本轮均为文档/工具层，14 分册判据与 addon 行为零变更。

## v2.5.9（2026-09-21）

> 公开脱敏注记（2026-09-21，GitHub 公开发行时补）：示例路径/配置示例/来源标注中的
> 本机用户名已替换为占位符（6 文件：docs 工单×1、11 §1.1×2、03/07 示例×2、
> render_check.py 默认输出改 tempfile.gettempdir()）——语义零变更，payload 哈希
> 相应更新。

v2.5.8 验收一项中危整改（F-163：随包 M13 自检判定依赖环境编码变量），版本取 **patch**
（探针 harness 修正，交付契约零变更、零工件变更——bundle 与两 wheel 逐字节同前版）：
- **F-163**（中危）：P-NL161 期望标记为中文串，子进程管道按平台编码（GBK）打印、
  harness 按 UTF-8 解码 → 默认环境 8/9（exit 1）、`PYTHONUTF8=1` 才 9/9——同一脚本
  同一机器两种结论，且该自检是发布闸附加项与稳态宣告条件②的唯一载体 → 整改取
  审计建议①+③并用：**run() 注入 UTF-8 环境**（子进程打印与父进程解码对称，对所有
  探针生效）+ **P-NL161 期望标记改 ASCII `LEDGER-INVARIANT`**（判据层去环境依赖，
  与其余 7 条同构）；脚本头增 F-163 纪律（中文标记须在未设编码变量环境实测）。
  **环境独立性实证（复刻审计同款实验）**：默认环境（env -u 剥离变量）与
  PYTHONUTF8=1 双跑均 9/9 exit 0；①查 定义句同步补 EXPECTED_LEDGER_ROWS 登记
  （审计 §7(a) 建议）。
- **F-164**（低·自抓）：④a 编号提取正则把 UTF-8 等词内的「F-」+数字片段误当发现编号（本条目
  首提「UTF-8 解码」即触发误报——自抓于发布闸）→ 提取正则加负向后视边界；
- 台账 F 行（宽式）134 → **136**（F-163/F-164 两行）；条款 **86** 不变。
> 稳态路线（方案 A）：v2.5.9 为规则变更轮（探针 harness 修正）；本版 **F-163 已
> 闭合且为任何稳态宣告的前置修复**——若 v2.5.9 轮零新增发现，则计入宣告对照
> **(v2.5.9, v2.5.10)**，两轮零新增发现 + 随包 M13 自检全绿（环境无关）即宣告稳态。
> 〔F-153 同族显式闭合（v2.6.0 注）：上句条件未成立——v2.5.9 轮有 F-165/F-166 两项
> 新发现；现行候选对照见 v2.6.0 条目〕

## v2.5.8（2026-09-21）

**第二拍候选轮**：收 v2.5.7 登记暂挂的两项低危（F-161/F-162），版本取 **patch**
（一行常数断言 + 权威文本同步；交付契约零变更、零工件变更）：
- **F-161**（低）：台账集登记断言落地——`check_ledger_invariants` 增
  **EXPECTED_LEDGER_ROWS=134**（宽式口径）登记断言，台账行被静默增删即
  「台账集登记断言失败」exit 3；负例实测：删 F-100 历史行 → exit 3 + 指名
  133≠134。随包探针集新增 **P-NL161** 同款破坏探针，并顺带修复探针脚本的
  路径 bug（台账类探针须跑**副本自带** runner，否则对原版 HERE 不可见）；
  M13 自检 8/8 → **9/9**；
- **F-162**（低）：退出码语义传导——runner docstring 退出码表改「2 = 表格解析失败
  **或条款集登记断言失败**（EXPECTED_CLAUSES，F-159）；3 = …**或台账集登记断言
  失败**（EXPECTED_LEDGER_ROWS，F-161）」；①查 定义句同步补条款集登记语；
- **宣告时无 ⏸ 暂挂**（审计 §7(三) 建议采纳）：稳态宣告条件补一句「宣告时刻包内
  无 ⏸ 暂挂行」——本版兑现 v2.5.7 的两行暂挂后，包内 ⏸ 为零；
- v2.5.7 条目的条件句显式闭合（F-153 同族预防）。
- 台账 F 行（宽式）**134** 不变（F-161/F-162 为状态行原位转 ✅，M14 将记 2 行被改，
  依据台账纪律「处置后标记 ✅ 并保留一行历史」）；条款 **86** 不变。
> **稳态宣告条件自证（HANDOFF §九 方案 A）**：第一拍 v2.5.7 审计零新增发现已成立；
> 本版若审计同样零新增发现 + M13 自检全绿，则 **(v2.5.7, v2.5.8) 宣告对照完成，
> v2.5.8 宣告为稳态版本**。宣告时 ⏸ 暂挂行 = 0。

## v2.5.7（2026-09-21）

**纯登记轮（v2.5.6 审计 §8 过程观察建议）**：零断言/零规则/零工件变更——仅版本号、
台账登记与本条目。目的：使本轮「零新增发现」概率最大化，作为稳态宣告对照
**(v2.5.7, v2.5.8)** 的第一拍（方案 A：连续两轮零新增发现 + 随包 M13 自检全绿）。
- **F-161**（低）：台账行可被静默删除（F-159 登记断言的非对称——条款集有、台账集
  无；删历史行实测 ①查全绿）→ 本轮登记暂挂，整改并入 v2.5.8；
- **F-162**（低）：条款集断言的退出码语义未传导到权威文本（runner docstring/①查
  定义段）→ 本轮登记暂挂，整改并入 v2.5.8；
- v2.5.8 计划：收 F-161/F-162（EXPECTED_LEDGER_ROWS 常数 + 权威文本 1~2 行同步）。
  〔F-153 同族显式闭合（v2.5.8 注）：上一条目「若 v2.5.6 轮零新增发现则计入对照
  (v2.5.6, v2.5.7)」的条件未成立——该轮有 F-161/F-162 两项新发现；现行候选对照见
  v2.5.8 条目〕
- 台账 F 行（宽式）132 → **134**；条款 **86** 不变。
> 本轮对照审计方 v2.5.6 报告 §8「过程观察」：稳态判定连续 6 版卡在 ⑨（修复自引新
> 缺口），纯登记轮即为此设计的刹车。

## v2.5.6（2026-09-20）

v2.5.5 验收三项低危整改（F-158/F-159/F-160，全部 1 行级），版本取 **patch**：
- **F-158**：恒真 lint 的诊断文案被 `if not matched` 无条件覆盖（拦截生效但原因不
  显示，VACUOUS PATTERN 从不出现在输出）→ 改 `if not matched and not detail`；
  负例实测：注入 re:x* 条款 → FAIL + VACUOUS PATTERN 诊断正常显示；
- **F-159**：条款集可被静默缩删（删 #50/#66 后 ①查与随包自检双绿）→ runner 增
  **条款集登记断言**（EXPECTED_CLAUSES=86 + 编号 1..86 连续性，违反 exit 2）；
  负例实测：删中段 #50 / 删末行 #86 → 均 CLAUSE-SET CHECK FAIL；新增/删除条款
  须同步修改该常数，属刻意的登记动作；
- **F-160**：v2.5.5 条目「台账 F 行 122 → 126」两端皆错（起点为 v2.5.3 陈旧基线）
  → 改宽式口径 125 → **129** 并注明口径（F-35 家族第 6 次的收口方式：计数以 runner
  输出为准，散文数字逐步退出）。
- 台账 F 行（宽式）129 → **132**；条款 **86** 不变。
> 稳态路线（方案 A）：v2.5.6 为规则变更轮（runner 两处断言增强）；若 v2.5.6 轮
> 零新增发现，则计入宣告对照 **(v2.5.6, v2.5.7)**——两轮零新增发现 + 随包 M13
> 自检全绿即宣告稳态。

## v2.5.5（2026-09-20）

v2.5.4 验收四项整改（F-154~F-156 + F-157，1 中危 + 3 低危/元层），版本取 **patch**
（指纹锚与判据工具修正，交付契约零变更、零代码变更——bundle 与两 wheel 逐字节同前版）：
- **F-155**（中危·P0）：**v2.5.4 的 #85 re: 链式锚是恒真断言**——单元格内层竖线单层
  转义被 load_table 还原成裸竖线，正则语义变「或」，一支可匹配空串（P96 实测空串/
  无关文本/他文档全匹配）。**同时更正虚报**：v2.5.4 台账/CHANGELOG 所称「负例三连
  exit 1」实为验证脚本输出重定向失败产生的假退出码，runner 从未执行——按 F-40/F-45
  家族如实入账。整改三管齐下：① #85 改**双层转义**恢复链式语义（逐段二分修复一处
  缺空格断点）；② runner 增**恒真 lint**（re: 条款 pattern 可匹配空串 → VACUOUS
  PATTERN 按 FAIL）；③ **M13 探针集固化为随包自检 `evals/run-negative-probes.py`**
  （7 破坏探针 + 正例对照，8/8 PASS；发布闸建议与 ①查同跑）。负例重验：审计同款
  P90/P91/P92 破坏全部 FAIL #85 exit 1，输出落在可写路径并核对内容；
- **F-156**（低·元层）：「零变更轮」与「修复轮」定义互斥致宣告条件持续后退 →
  **采纳审计方案 A**：宣告条件改为「**连续两轮零新增发现 + 随包 M13 自检全绿**」
  （修复轮不引入新发现即可计入）；HANDOFF §九已改写。自 v2.5.5 起起算；
- **F-154**（低）：v2.5.3 条目稳态路线段补勘误（候选对照已被取代，交叉引用本条目）；
- **F-157**（低）：#86 指纹去 `**`（F-36 语义指纹规范），SKILL 围栏行同步去粗体；
  「逐条款格式扫描」自本轮起列为发布闸固定项。
- 台账 F 行（宽式口径）125 → **129**〔F-160 勘误（v2.5.6）：原记 122 → 126 两端皆错——起点用了 v2.5.3 陈旧基线；口径=runner 宽式计数〕；条款 **86** 不变（#85 重写锚、#86 换语义串）。
> 审计侧事件备查：v2.5.4 轮审计对 #85 恒真的发现使用了「解析器取 pattern + 空串
> 匹配」的机制证明（P96）——该手段已吸收为 runner 内建 lint，外部审计发现转为
> 包内永久防线，是「稳态可自证」路线的第一块基石。

## v2.5.4（2026-09-20）

v2.5.3 验收三项低危整改（F-151/F-152/F-153），版本取 **patch**（指纹锚替换 + 分发件
卫生 + 元层裁定，交付契约零变更、零代码变更——bundle 与两 wheel 逐字节同 v2.5.3）：
- **F-151**：#85 字面量锚只护表前引导句，表体四行可被静默删除/篡改（P81/P83 实测
  86/86 PASS，恰为 v2.5.2 缺陷复现不可检出）→ #85 换 **re: 链式正则锚**：引导句→
  禁令→核心判据→四行有序全链，删任一行/改处置列即断链。负例三连实测：删四行/
  改第 2 行处置列/只删首行 → 全部 exit 1；正例 86/86 PASS。**本项目首个 re: 模式
  条款**（parser 自 v1.5 起支持，首次实用）；
- **F-152**：F-150 的点状修复残留两处（v2.5.0「通道迁移状态」段、v2.4.0「本机
  runtime」句）→ 改中性口径并统一注记；:59 经核对为历史记述保留；
- **F-153（元层裁定，采纳审计方案 (b)）**：稳态宣告条件④的「规则变更」**明确涵盖
  判据纠错与指纹补强**（不自我放宽）；v2.5.4 本身即规则变更轮（换 #85 锚），候选
  宣告对照顺延为 **(v2.5.5, v2.5.6)**——该两轮须均为零变更轮（仅版本号/台账/
  CHANGELOG）且零新发现，方可宣告稳态。裁定依据同写 HANDOFF §九；
- 台账 F 行 122 → **125**；条款 **86** 不变（#85 换锚不加条）。
> 附带（包外）：HANDOFF 抬头「当前为稳态」系 v2.2.0 时代旧表述，已改中性口径
> （与第 57 轮「稳态裁定=否」一致）。

## v2.5.3（2026-09-20）

v2.5.2 验收三项整改（F-148 中危 + F-149/F-150 低危），版本取 **patch**（判据文本与
分发件卫生修正，交付契约零变更）：
- **F-148**（中危·P0）：v2.5.2 围栏「判据顺序」的步骤①以「warning 非空」为条件，
  而除 `up_to_date:true` 外所有态都带非空 warning（审计用工具自身解释器五态枚举
  实证）→ 步骤②③永不可达，真过期被判「重试→排障」而非重装——F-147 误诊的镜像、
  修复自引新缺口连续第 2 版。整改：SKILL §0 围栏重写为**三态判据表**（`up_to_date`
  × `source` 四行对号，覆盖审计五态表全部形态）+ 核心判据句「只有 up_to_date:null
  才是握手瞬断」+ 明写「勿用 warning 非空作判据」+ 表外异形（字段缺失/纯报错字符串）
  分流出处；
- **F-149**（低）：围栏实质内容零指纹（审计 P70 删整段/P72 改通道名均 84/84 PASS）
  → 新增条款 **#85**（三态判据表）/**#86**（默认通道声明），两锚件内唯一。指纹 84 → **86**；
- **F-150**（低·元层）：本机环境状态与审计过程记述移出 CHANGELOG（包外 HANDOFF
  环境事实表已载），消除换机失准与包外悬挂引用——F-48 教训在分发层的落实；
- **稳态路线（审计 §13 建议，已录入 HANDOFF §九）**：v2.5.3 只收本三项不加新规则；
  下一版若为「零变更轮」（仅版本号/台账/CHANGELOG）且 M13 探针全绿、零新发现，
  与 v2.5.3 构成连续两轮零新发现，即可宣告稳态版本并把 M13 探针集固化为随包自检。
  〔F-154/F-156 勘误（v2.5.5）：本段候选对照已被取代——v2.5.4 收 F-151~F-153、
  v2.5.5 收 F-154~F-157（均为规则变更轮）；稳态条件已按审计方案 A 修订（去掉
  「零变更」硬条件），见 v2.5.5 条目与 HANDOFF §九〕
> 台账 F 行 119 → **122**；条款 84 → **86**。

## v2.5.2（2026-09-20）

v2.5.1 验收两项低危整改（F-146/F-147），版本取 **patch**（文档同步 + fork 一处错误分支语义修正，交付契约零变更）：
- **F-146**（低）：ATTRIBUTION 改写清单「用户引导文本」行随 v2.5.1 的 uvx 去名已过时
  → fork 列改 `brickfly-mcp install-addon` 等 + 注明变更缘由（F-141）；SHA256SUMS 随改重算；
- **F-147**（低）：`up_to_date` 三态被压成布尔——addon 握手瞬断与「确认过期」在围栏
  两个判据上同形（真机实测首次调用瞬断返回 false+protocol null+source:error，第二次
  正常），SKILL §0 围栏按字面会误诊「过期→重装」→ **双路径修复**：① fork 源码两个
  error 分支（握手异常/无效 payload）`up_to_date=False` → `null` 三态（`source=missing`
  确认过期仍 `false`），wheel 重建重装 + stdio 复验 BrickflyMCP/31 工具；② SKILL §0
  围栏改**判据顺序**：`source:"error"` 或 `warning` 非空 → 先重试一次再判，勿据
  `up_to_date:false` 直接判过期；
- ATTRIBUTION 新增「行为修正」节：如实登记 fork 相对上游的首处有意行为差异
  （即上述三态化）——parity 声明自此以「2.0.0 + F-147 行为修正」为准。
> 审计侧说明（记录备查）：v2.5.1 曾有一轮未落盘的审计（数字与实测不符），本轮验收
> 已作废重审；F-146/F-147 编号以首次落盘审计报告为准（验收报告按包首惯例留档于
> 审计侧 `_verify_*` 目录，不入包）。本机环境状态（通道切换/插件启用集）记录于
> 包外交接文件，不属包内保证（F-150 勘误）。

## v2.5.1（2026-09-20）

v2.5.0 验收五项整改（F-141~F-145，2 中危 + 3 低危）**+ v2.4.0 轮七项欠账补登记与处置
（F-134~F-140）**：
- **F-141**（中危·P0）：默认通道按名安装指令在干净机器必失败（fork 未上 PyPI；隔离环境
  实测 uv 解析 404；fork 自身 `update_command` 同病——作者验证链在已预装机器上跑故漏过）
  → INSTALL §1 表/§2、11 §1.1/§1.2 改「本地离线 wheel + 已装入口 `brickfly-mcp
  install-addon`」并明写「fork 未上 PyPI」；fork 源码 8 处引导文本去按名；wheel 重建重装，
  stdio 复验 BrickflyMCP/31 工具。
- **F-142**（中危·P0）：v2.4.0 轮 7 项发现（F-134~F-140）零登记 → 本版 7 行补登记
  （状态如实：4 未处置 1 部分 2 已闭合）；④ 定义段新增**「上轮验收未闭合项核对」**
  发布前人工清单项（F-69/F-70 同族第 2 次的治本动作）。
- **F-134 残留补齐**（部分→闭合）：11 §1.1 三处（安装命令行 1.9.1 注释、版本协议映射、
  ZCode JSON 仍指 blender-mcp.exe）全部对齐双通道口径；
- **F-135**（三处 26→31 失准，含审计侧自纠的双向教训）→ 改 **28 → 31、新增 3 个**，
  附可复算口径（`@mcp.tool(` 静态计数 / tools/list）；
- **F-136**（两处「自 X 起」归类失准）→ 06 改「safe mode 为 env 选入（上游一贯如此）」、
  INSTALL 改「自 v1.6 起即显式拒绝后台」；SKILL §1 注明本部署开启≠上游默认；
- **F-139**（④c 假阳性：反引号裸驼峰专名被按文件核验）→ 兜底分支收窄为「须带 :行号
  后缀」；负例双测：`PolyHaven` 不再误报、`INSTALLx:153` 仍拦；
- **F-140**（④b「单调递增」措辞残留）→ 定义段与 PASS 行改「严格递增」（历史行不回写）；
- **F-143**（fork wheel 缺 dist-info/licenses/LICENSE 而 METADATA 声明 License-File）→
  构建脚本带回该件（16→17 件），RECORD 自检过，bundle SHA256SUMS 重算，MIT 副本义务
  恢复完整；
- **F-144**（两条架构事实未落权威分册）→ 10-headless-ci 增「--python 全角路径」节、
  09 增「5A 驱动侧纪律：主线程/timer」节，配 **#83/#84** 指纹（82 → 84）；
- **F-145**（「parity diff 128 行」口径不可复算）→ CHANGELOG/U-05 行改述为可复算口径。
- 版本取 **patch**：全部为修错/补登记/文档与构建修正，交付契约与行为面零变更
  （fork wheel 变更仅为引导文本与 LICENSE 件）。
> 台账 F 行 105 → **117**；指纹 82 → **84**。下轮验收除常规项外，请复核「上轮未闭合项
> 核对」清单项的首次执行情况。

## v2.5.0（2026-09-20）

**U-05 上游解耦·S1 收编（用户令"开干"）**：默认通道切换为 **brickfly-mcp**——本 skill
把上游 mcp-for-blender 2.0.0 全量源码（~10,264 行）vendor 为自有 fork（MIT，仅身份字段
改写：发行名/CLI 入口/FastMCP 名/addon 模块名与 bl_info 名/用户引导文本；**协议 7、
31 工具、安全模式、五库逻辑逐字节不动**，parity 差异集中在 8 个文件 + addon 10 行，
逐行核对全为身份行——可复算口径：`git diff --no-index --numstat`（F-145 勘误：
原「128 行」数字不可复算，已改述））。决策依据与
三级路线见 `迷深清洗工作2/_blender_skill_上游解耦可行性报告_20260920.md`：
- **新增 assets/brickfly-mcp-bundle/**（fork addon v1.7 + server 2.0.0 wheel + LICENSE +
  ATTRIBUTION 改写清单 + SHA256SUMS），上游包保留为回退通道；
- **验证链五层**：parity diff → wheel RECORD 自检 → uv tool 双工具并存 → stdio 直驱
  （initialize=BrickflyMCP、tools/list=31）→ **fork server↔fork addon 端到端**
  （up_to_date true/协议 7/7/get_scene_info 实时/视口截图 image/png 346KB；addon 侧
  by-exclusion 证明——会话内停用上游 addon 后 fork 独占应答）；
- **文档通道事实**：INSTALL（默认/回退双通道路径表+迁移指引）/11 分册（§1.4 双通道与
  供应链：fork 发布闸自守、上游 addon 互斥启用、发布监测不自动跟随）/06/SKILL §6
  （面板双标签）；
- **A1 围栏（S0 落地）**：SKILL §0 第 2 步版本期望值（addon [1,7]/协议 7；不符按处置、
  漂移即告知勿假称兼容）+ HANDOFF §九"上游变更→评估吸收"触发；
- **新增条款 #81/#82**（围栏与通道声明），指纹 80 → **82**；台账 +U-05 行；
- **验证中新记录的两条架构事实**：① `--python` 脚本路径含全角字符时 Blender 解析失败
  （"could not be opened"，用 ASCII 路径规避）；② 驱动/验证脚本在 Blender 主线程
  sleep 会堵死 addon 命令队列（timer 架构约束）——必须让主线程回到事件循环；
- **通道迁移**：客户端与服务端的切换路径见 INSTALL §2/§3（本机迁移实况记录于
  包外交接文件，不属包内保证——F-150/F-152 口径统一）。
- 版本取 **minor**：新增自有通道与 bundle 资产（增量），交付契约（宣告行/△/N/A/降级
  出口）与行为面（parity）未变，非 major。架构专项审计按 F-55~F-58 规则在下轮验收
  作为固定审计项。
> 勘误（修订传导补漏）：SKILL §0 第 1 步 V-01 注记曾残留"内附的 1.9.1"，v2.4.0 改写
> INSTALL 时未同步 SKILL 侧——本版一并改为 2.0.0 口径。

## v2.4.0（2026-09-19）

v2.3.1 验收十项整改（F-124~F-133，2 中危 + 8 低危）+ **上游 mcp-for-blender 2.0.0 适配**：
- **F-124**（中危·P0）：④ fail-closed 名不副实——不可核验消息走 warnings 通道印成
  "仅形态提示不阻塞"，双不可解析实测 exit 0（P34）→ runner fatal/warnings 分通道，
  不可核验一律 exit 3；CHANGELOG **首个二级标题**非 `## v` 形态即 FAIL（防 ④a 静默
  降级解析次新条目）；metadata.version 不可解析显式 FAIL；新增条款 **#80** 标题形态锚。
  负例实测：双不可解析 → 2 问题一次性报出 exit 3。
- **F-125**（中危·元层·P0）：v2.3.1 台账 11 行中 4 行名实不符（F-115 判据已立未用）→
  双路径落地：**④c 断言实作**（台账行反引号指名的文件必须存在，`check_ledger_file_refs`，
  负例实测 exit 3；`deploy-roots.local.txt` 按 F-48 豁免）+ 4 行原位勘误 + 14:10 步序残留补改。
- **F-126**（低危）：5 处无指纹文本补条款 **#75~#79**（SKILL 内嵌展示 / 00 视觉规格摘录 /
  fragments 渲染预览图 / 14 G3G4 机验门 / 14 前提翻转），五锚件内唯一。指纹 74 → **80**。
- **F-127**（低危）：13 §1 穷举清单扩至 **15 项**（补 14 §1 感知前提自检、14 §5.3 案例视觉引用）。
- **F-128**（低危）：删除 F-119 所补的无效存在性断言（共漂移实测 ④ 全绿、无法独立检出），
  ④ 定义段与 runner 注明**共漂移由 ②查负责**（沿 F-122 教训句），不留虚假保证。
- **F-129**（低危）：台账 F-116 行目标版本勘误 v2.2.14 → v2.3.1。
- **F-130**（低危·元）：v2.3.1 条目三处失实勘误（指纹计数/归因、F-116 失实记载、漏列 F-122）。
- **F-131**（低危·分发）：交付目录 `blender-mcp-v2.2.14-hotfix.zip` 改名
  `blender-mcp-v2.3.0-hotfix1.zip` 并入账——恢复"文件名版本↔内容版本"单一映射。
- **F-132**（低危）：06/SKILL 禁用态描述按真机实测重写——工具由 server 静态全量注册，
  禁用态直调检索工具返回 `Unknown command type`（形似链路故障勿误排障），引导文本仅在
  get_*_status；开关改动即时生效无需重连（SKILL §7 同族表述一并修正）。
- **F-133**（低危）：06/14 通道事实按真机实测改写——PolyHaven search 文本**不含任何 URL**
  （server 格式化丢弃 thumbnail_url），"URL 落盘实看"路径在 MCP 通道内不成立；可行路径 =
  下载低分辨率资产→渲染→实看 + △ 出口（上游 2.0.0 源码同构复核一致）。
- **上游 2.0 适配（minor 依据）**：`blender-mcp` → **`mcp-for-blender`**（2026-09-16 发布
  2.0.0，旧名兼容）。工具 **28 → 31**（F-135 勘误；计数口径=server.py `@mcp.tool(` 静态
  计数或客户端 tools/list；新增 3 个：`describe_node_type`/`bpy_api_lookup`/
  `export_scene`，`disable_telemetry`/`record_trajectory_feedback` 于 1.9.x 已存在），协议 **5 → 7**，
  bundle addon v1.6 → **v1.7**、离线 wheel 1.9.1 → **2.0.0**（均取自 PyPI 官方 wheel，
  SHA256SUMS 重算）。INSTALL/06/ATTRIBUTION 同步：改名与兼容说明、safe mode 转为
  env 选入开启、telemetry 默认开与关闭途径、Poly Pizza licence/署名事实、addon 拒绝
  后台模式启动。**真机验证**：stdio 直驱 2.0.0 server——initialize 握手、tools/list
  实测 31 工具、get_addon_status 对运行中实例回报 protocol 5/expected 7（版本协商正确）；
  addon 1.7 已落盘 Blender 用户级 addons 目录（官方 CLI 安装，自动留 .bak），待下次
  Blender 重启生效。服务端升级后旧命令名 shim 的兼容做法见 INSTALL §3（本机迁移
  实况记录于包外交接文件——F-150/F-152 口径统一）。
- 版本取 **minor**：接入上游 2.0 能力面（新工具事实、协议 7、addon 1.7）+ 新增条款
  #75~#80 + 清单扩容，属增量条款；宣告行/△/N/A/降级出口等交付契约未变，非 major。
  **#02 换锚**：V-01 归因句随 2.0 适配改写（`内附的 blender-mcp` → `内附的
  mcp-for-blender`），①查自身拦获、指纹随文换锚（#65 先例）。
  审计建议项（F-113 工具侧：为 PolyHaven/Poly Pizza 补缩略图回图工具）仍未实施，
  维持"建议"级，待与上游 2.x addon 结构稳定后单独立项。

## v2.3.1（2026-09-19）

v2.3.0 验收十一项整改（F-113~F-123，3 中危 + 8 低危）：
- **F-113**（中危）：资产检索实看义务补替代路径与范围收窄（06/14 双侧）；登记 13 §1 清单；
- **F-114**（低危）：穷举清单扩容覆盖 14 分册新增义务（13 项）；
- **F-115**（中危）：④ 定义段补"机械部分含 ④a/④b 两条断言"两分句；
- **F-116**（中危）：CHANGELOG 缺失/不可解析时 ④a/④b 显式 FAIL（fail-closed）；
- **F-117**（低危）：14 §3 门判据按 00 分册分工限定（G1/G2 目验、G3/G4 机验）；
- **F-118**（低危）：7 处新增实看义务文本配指纹（M13 探针 P4~P10 证实可静默删除）；
- **F-119**（低危）：④b 口径改"严格递增"（重复行不检出→显式报错）；frontmatter 版本标题断言；
- **F-120**（低危）：INSTALL docs/ 地图补"决策模型映射报告"；
- **F-121**（低危）：SKILL §0 感知自检步序号勘误（实作第 3 步非第 4 步）+ F-112 教训句限定；
- **F-122**（低危）：F-112 教训句加限定——F-13 类版本失同步的唯一防线是 ②查
  （--expected 须由发布意图给出），原句"四查全跑"对该类结构性无效；
- **F-123**（低危·元）：F-123 裁定记录入 CHANGELOG（前提翻转属感知前提强化而非契约变更）。
新增条款 **#71~#74**（4 条新指纹）；#65 换锚（跨行指纹，F-94 同族）。指纹 70 → **74**。
> 勘误（F-124/F-125/F-129/F-130，v2.4.0 补记）：本条目原有三处失实——① 指纹计数
> 误写"#66/#67 新增、70→72"（实增 #71~#74、70→74）；② F-116 的"显式 FAIL + 标题形态
> 指纹"未真正落地（实为非阻塞警示且指纹不存在，v2.4.0 实作闭合）；③ 台账 F-116 行
> 目标版本误记 v2.2.14（实为 v2.3.1）。F-118 行点名与实际锚点不符、F-120 行误记修复
> 位置（实为 `INSTALL:148`），均已在 v2.4.0 台账原位勘误（F-125）。

## v2.3.0（2026-09-19）

**视觉深度融合（用户反馈 U-04）**：主张"必须以模型有原生多模态能力为前提，视觉融入
每一个流程"。此前视觉是散布的验收动作（U-03 系列已修五处盲区），本版翻转为
**默认开启的一等感知通道**，新增 references/14-vision-workflow.md 作为全流程编排权威册：
- **§1 感知前提**：SKILL §0 新增感知自检步（实作位于 §0 第 3 步，F-121 勘误）（固化截图+实看，确认通道+场景第一印象）；
  降级定位翻转为异常路径——开场声明/宣告行 △/高视觉依赖工序降级下不执行；
- **§2 开场识别**：场景视觉盘点（所见清单入 report——回答"看起来是什么样"）+
  参考规格提取（实看后剪影/配色/材质性格进 PRD 卡片）+ 意图确认闭环；
- **§3 过程实时闭环**：循环从"读→改→验→汇报"细化为"读→改→**看**→修→复看→汇报"，
  修正指令必须引用所见；
- **§4 成品展示义务**：交付内嵌最终渲染图+视觉描述，有参考则对照展示；
- **§5 知识库视觉**：资产库检索缩略图实看（外观匹配是选择依据）+ 组件渲染预览图约定。
版本取 **minor**：新增分册与义务（增量条款），交付契约格式未变、降级路径保留故非 major。
**F-123 裁定记录**：前提翻转（视觉默认开启）属感知前提强化而非交付契约变更——宣告行
格式、△/N/A 三态、降级出口均沿用既有定义，故维持 minor；此理由入档备审计。
新增条款 **#68/#69/#70**。指纹 67 → **70**。
> 勘误（F-121）：SKILL §0 感知自检实作位于第 3 步（原第 3 步"报错→§6"顺延为第 4 步），
> CHANGELOG 与 14 分册曾写"第 4 步"，以 SKILL.md 实际步序为准。
> 勘误（F-112）：上版 v2.2.14 的 zip 内 frontmatter/INSTALL 停在 2.2.13（三处同步漏执行，
> 该轮 ②④ 查被跳过未拦住）——本版三处同步到位并补此勘误注；详见台账 F-112。

## v2.2.14（2026-09-18）

v2.2.12 验收四项低危整改（F-104~F-107，均为记录表/元层自身属性）：
- **F-104**：台账三行插入位置破坏"按编号重排"→移至 F-100 后恢复单调；
- **F-105**：新写自述两处小瑕（反引号未闭合/④ 定义段旧称）→对齐实作；
- **F-106**：审计方法标签包内无定义→台账 preamble 加 M13/M14/Pn 定义；
- **F-107（治本）**：**④b 断言实作**——台账 F 编号单调递增 + CHANGELOG 最新条目版本
  == frontmatter 版本（负例实测触发），一次闭掉 F-104/F-107 两个同源缺口与
  F-92/F-101 的"缺条目"复发根因。
指纹 **67** 不变（#67 于 v2.2.12 已入，本版无新条款）。

## v2.2.12（2026-09-19）

v2.2.11 验收三项低危整改（F-101/F-102/F-103）：
- **F-101**：补本条目（v2.2.11 曾漏条目，F-92 同形复发）——三项内容：#65 换锚规则句
  本体、④ 机械断言 `check_changelog_fnn_registered` 实作、F-100"丢失→错位"勘误；
- **F-102**：④ 机械断言覆盖域声明（仅覆盖 CHANGELOG 所载项，未载项人工核验）写入
  ④ 定义段；runner docstring 退出码表补 3；`summary④` 标签改名 `summary④a(台账登记断言)`；
- **F-103**：清单枚举体补指纹 **#67**（`②00 E 型动画帧抽查`@13，件内唯一——锚枚举体
  本体防整段消失，第 4 次锚错位家族的收敛项）。
> 勘误（v2.2.13）：v2.2.12 打包时 F-105a 反引号修复、台账 F-104 移位与 ④b 断言实作均在发布后完成，见 v2.2.13 条目。
指纹 66 → **67**。

## v2.2.11（2026-09-19）

v2.2.10 验收两项低危整改（F-99/F-100）+ ④ 查治本机械断言首次实作：
- **F-99**：#65 换锚规则句唯一子串 `必须同步 △ 出口，不得只立义务不给出路`
  （M13 P3/P7 实证锚在语义本体，锚错位链条断开）；
- **F-100**：CHANGELOG "U-03 追加行丢失"一词勘误为"错位"（M14 跨版追踪：该行五版
  内均在且逐字节相同）；
- **治本实作**：`run-regression-checks.py` 新增 `check_changelog_fnn_registered`——
  ④ 查机械断言"CHANGELOG 最新条目的 F-NN 必须已在台账登记"，缺口 exit 3；
  **首跑即抓出 F-100 漏登**（断言自证有效）。
指纹 66 条不变（#65 换锚）。

## v2.2.10（2026-09-18）

v2.2.9 验收三项低危整改（F-96/F-97/F-98）+ 台账重建：
- **F-96**：视觉义务点穷举清单补第 7 项 ⑦SKILL §4 视觉核验（原清单自称六处实测七处）；
- **F-97**：新增条款 **#65** `视觉义务点穷举清单`@references/13 与 **#66** `实际打开 PNG
  查看内容`@SKILL.md（四处新增机器可读文本按 M13 P4~P7 证实删除不告警；两条新指纹按
  第四项自检反向实测——单点删除均触发对应条款 FAIL）；
- **F-98**：F-93/F-94/F-95 三行补登记台账（目标版本 v2.2.9）；
- **台账重建**：修复台账区行序错乱（历史编辑导致 U-03 追加行**错位**、F-74/F-91 等行
  错位——该行本体五版内均在，F-100 勘误），72 行按发现编号重排、无缺无重。
指纹 64 → **66**。

## v2.2.9（2026-09-18）

v2.2.8 验收三项低危整改（审计 M13 反向实测深挖出的配套缺口）：
- **F-93**：△ 降级出口余 2 处义务点未覆盖 → 13 卡 0 补 deviation 专属出口（G0 判据
  交付门 △ 无处记录，走 deviation 留痕）、00 E 型补 △ 短指针；**视觉义务点穷举清单**
  成文（六处，13 §7）——新增实看义务必须同步 △ 出口；
- **F-94**：#63 换锚的覆盖代价（△ 本体全量消失不告警+条款名名实不符）→ 00 规格行
  去重复字面使 `R1△(视觉未核验:` 件内唯一 → #63 锚回含 △ 本体的唯一串；HANDOFF
  第四项自检探针对象改为"被保护的语义本体"；
- **F-95**：F-85/F-86 处置版本勘误（实为 v2.2.7，台账曾记 v2.2.8）→ 台账两行目标版本
  改 v2.2.7、F-90 表述与 CHANGELOG 同步。
指纹 **64** 不变（#63 换锚 + 规格行修订，条数不变）。

## v2.2.8（2026-09-18）

v2.2.7 验收六项低危整改（F-87~F-92，全部为整改动作自身的配套）：
- **F-87a**：#63 指纹改件内唯一串 `宣告行标`（审计 M13 反向实测：原指纹缺定位力，
  定义处单点回退/删除均不可检出）；**F-87b**：补 **#64** `产出图同样须实看`@references/12；
- **F-88**：RPD 报告 §二「事实核查」与文首同措辞（时点限定）；
- **F-89**：RPD 报告 §四表头"五处"→"四处"（档位显式化已并入①），§五.3 同步；
- **F-90**：台账补 F-84/F-85/F-86 三行（F-85/F-86 实处置于 v2.2.7、F-84 文首部分亦为
  v2.2.7——v2.2.8 台账曾误记版本归属，本三行已按 F-95 勘误）；
- **F-91**：SKILL §4 与 12 §3 实看义务各补 △ 降级出口回指（视觉通道不可达时标 △）；
- **F-92**：本条目即 v2.2.7 叙事补齐；v2.2.6 条目内改动已标注版本归属（见下）。
指纹 63 → **64**。

## v2.2.7（2026-09-18）

十项整改一次性落地（F-77~F-83 + F-84~F-86 + 附篇方案 B 采纳）：
- F-77 传导三漏网（13 §7 △ 出口 / §6⑥ 三态 / 工具名中立化补漏）；F-78 R1△ 统一半角；
  F-79 删 12 分册重复快渲行；F-80 §6 引用限定 SKILL.md；F-81 去重；
- F-82 版本语义行扩充裁定（字段增补属 patch、立版级才 major）+ v2.2.5 勘误注；
- F-83 台账六行补登记 + 删绝对化末行（台账纪律违例自纠）；
- F-84 RPD 报告 grep 声称补时点限定；F-85 删无法核实的消歧句；F-86 INSTALL docs/ 地图补类；
- 附篇方案 B 采纳：RPD 报告改写为"两个体制、同一条公理"（简单任务单方案回退/复杂任务
  M6 三方案串行择优），偏离①强化为情境建模权外置、新增④生成-择优外置；
  architecture-brickfly-v3.md:169 "并行择优"勘误为串行。
新增条款 **#63**。指纹 62 → **63**。
> 版本归属注：v2.2.6 条目内"报告入库前全包 grep 零命中"的限定语，实为 v2.2.7 所作
> （F-92 勘误标注）。

## v2.2.6（2026-09-17）

新增 `docs/decision-model-rpd-mapping-20260917.md`：决策模型映射报告——以 Gary Klein 的
RPD（Recognition-Primed Decision）框架审视本 skill 决策机制。结论：报告入库前全包 grep RPD 零命中（入库后命中项仅为该报告自身与 CHANGELOG 本条目）
（不存在字面漂移）；决策机制与 RPD 高度同构（分型问题池=模式库、推倒重来=单方案回退、
档位=快慢分层），三处偏离（用户治理/实机沙盘/清单兜底）均为有意设计立场——定性为
"外部化治理版 RPD 变体"。纯文档新增，无行为变更。

## v2.2.5（2026-09-17）

v2.2.4 验收三项整改（审计首次具备视觉实测条件，端到端链路全通）：
- **F-74**（中危）："必须实看"硬性义务无前提与降级，而"看不了"是实测可达状态
  （路由未声明图像输入/视觉桥限流/纯文本客户端）→ R1 视觉层补**感知前提** +
  **降级条款**（不可达时标 `R1△(视觉未核验:<原因>)`、注明未经核验、建议切换图像路由
  ——第三种合法行为，不违规不卡死；链路故障才走 §6 排障，两态区分）+ 工具名客户端
  中立化（SKILL/00/13 三处）；
- **F-75**（低危）：交付宣告行 R1 标记扩为 `R1✓(路径;视觉✓)`——全链唯一机器可查行
  承载视觉层证据；视觉不可达标 `△+原因`；
- **F-76**（低危）：R1 视觉层义务句补指纹 **#62**（`必须经视觉通道`@00）。
指纹 61 → **62**。
> 勘误（v2.2.7 裁定）：本版含新增条款 #62（minor 成分），按扩充后的版本语义应属
> minor；版本号已发不追改，特此注记。

## v2.2.4（2026-09-17）

**U-03 同族全量检查**（用户令"仔细全量检查一遍"）：感知断链不止落盘图一处，四面齐修——
- **A 参考图实看义务**（最重，建模源头）：G0 出口"参考图就位"≠模型看过；文件路径形态
  的参考图无条款要求打开。13 卡 0 改"就位**且模型已实看**（文件路径形态必须用文件读取
  工具打开实看）"——没看过参考图，一切"对照参考"均为空谈；
- **B 过程层截而不看**：12 分册眼快扫/快渲两行补实看义务（"截而不看 = 没截"，
  产出图 Read 或截图通道实看）；
- **C 动画交付视觉核验**：00 E 型池补首/中/尾帧 Read 抽查（运动/循环衔接禁止凭
  keyframe 数字想象）；
- **D R5 视觉证据口径**：视觉类验收条款以 R1 视觉核验的所见描述为证据，report 数字
  不构成视觉条款的证据。
新增条款 **#60/#61**。指纹 59 → **61**。

## v2.2.3（2026-09-16）

**质量门视觉核验**（用户反馈 U-03："最终质量门模型像瞎了一样"，预设模型有原生多模态）。
根因：G4/R1 的机验只有"文件存在+尺寸非零"（`bpy.data.images.load` 回读），**像素从未
进入模型视觉上下文**——多模态能力再强，没人喂图就是瞎。整改（双层判据）：
- **R1 升级为渲染证据 + 视觉核验**：存在性层（回读）之外，必须经视觉通道实看图像——
  多模态模型用文件读取工具（Read）打开渲染 PNG，把"看见了什么"写入交付说明；
- 13 分册 §7 剪影测试/停手判据同规（剪影"可辨认"须以实看为前提）；
- SKILL.md §4 工作循环挂钩："**禁止凭 report 数字想象画面**"。
新增条款 **#59**（`禁止凭 report 数字想象画面`@00）。指纹 58 → **59**。

## v2.2.2（2026-09-16）

F-73 整改（DSH 侧 v2.2.1 审查发现，低危）：F-69 为无限挡新增"过目并待确认"停顿点，
开工提示模板未同步预告（仍称"不再逐问打扰"、未提预授权出口）——义务>预告，与 F-68
（承诺>义务）反向成对，且停顿正是 U-02"分不清在干活还是卡住"的高发时刻。
模板括注按审计措辞补预告：无限挡"请您过目确认后再继续——希望连续执行可一开始就说
'预授权连续'"。新增条款 **#58**（`请您过目确认后再继续`@00）。指纹 57 → **58**。

## v2.2.1（2026-09-16）

v2.1.9/v2.2.0 合并验收四项低危整改。**含一次台账纪律违例的自纠**：F-69/F-70 发现于
v2.1.9 审计，v2.2.0 功能版未承接也未登记（违反"已知暂缓必须登记"），本版补登记并处置：
- **F-69**：无限挡行"完整档全集+每门过目"自指冗余（A+x 且 x∈A，过程门列失去区分力）
  → 改真增量："每门**过目并待用户确认**方可进下一阶段（可预授权连续）"；
- **F-70**：#55 指纹命中 2 处失去定位区分力（F-51 同类）→ 改件内唯一串
  `与开工提示承诺对齐`；
- **F-71**：部署五根各含 2 个 `__pycache__` 字节码残留（③查仅 WARN，"根==包"被打破，
  V-06 同源）→ `check-deploy.py` 把 `__pycache__`/`.pyc` 与 `.bak` 同列 **FAIL**，
  五根现场已清理；
- **F-72**：收工清单对快速档不可达（必读止于 §1~§2 而清单位于 §8）→ 00 交付宣告附近
  加"**收工三问**"通用钩子（全档可达），细则回指 13 §8；#57 指纹锁定。
**第三项自检制度化**（审计 §三 中优先级建议）：契约表列修订后回看逐档递进性 + 指纹
唯一指向。新增条款 **#57**。指纹 56 → **57**。

## v2.2.0（2026-09-16）

**知识沉淀组件层首次扩容**（用户检查触发：DSH 首批实测 97 个阶段脚本的组件沉淀为零，
复用阶梯 L1/L2 层空转）：
- 新增积木 `fragments/bake_essentials.py`（selected-to-active 烘焙要点，ERR-014 心智
  模型内建）——无头 factory-startup 幂等双跑验证：四通道两轮全 ok、产物落盘一致；
- 新增积木 `fragments/lod_chain.py`（DECIMATE 比例链+apply+GLB 导出回读）——同款
  验证：两轮面数一致（35773/21961/9422）、对象零漂移、GLB 导出成功；
- 新增 `docs/worked-example-tank-denia.md`：首个 worked example（豹2A7 硬表面+达妮娅
  角色双管线，"摘要+明锚"指向工作区源档案；含 OptiX 实测数据与踩坑→教训去向索引）；
- **收工清单**入 13 分册 §8：①可复用组件提升（无头幂等双跑验证）②worked example
  整理 ③踩坑回填 ERR 表——组件层沉淀从此有制度化出口；
- 图集贴花不收录说明（numpy 依赖，安全模式禁；README 注明走独立通道）。
新增条款 **#56**（`是否有可复用组件`@13）。版本取 **minor**（新增可复用工具与条款）。
指纹 55 → **56**。

## v2.1.9（2026-09-16）

v2.1.8 验收两项低危整改（均为新增交互协议自身的声称一致性）：
- **F-67**：SKILL.md 称资产库问项"A/B/C 型已含"实作仅 A/B → **C 型池补问项**
  "贴图/HDRI 来源：启用哪些集成库？"（含探开关报状态义务）——C 型恰是素材库最相关
  的一型；#54 指纹锁定；
- **F-68**：开工提示模板承诺"完整档/无限挡"均看图但契约表仅无限挡有此义务
  （对外承诺大于对内义务）→ **契约表完整档行补"每门产出物交用户过目"**让承诺兑现
  （取建议①而非收窄承诺——U-02 的本意就是让用户看见过程）；模板括注微调覆盖
  G3/G4 报告形态；#55 指纹锁定。
**新自检两项制度化**（审计 §七.4）：①声称覆盖面回溯——凡写"X 型已含/均已"须逐型
核对实作；②对外承诺与契约表对齐——凡模板承诺须在契约表有对应义务。
指纹 53 → **55**。

## v2.1.8（2026-09-16）

**开工提示**（用户反馈 U-02）：问答全部结束后，agent 静默执行建模工作时间较长，
用户无法分辨"在干活"还是"卡住/忘了自己"。新增 00 分册「开工提示」条款：问答收口
（卡片确认 / 按默认开工 / 降档收口）进入执行环前，须输出开工提示——三要素缺一不可：
①声明问答已结束；②预告静默执行与期间形态（含质量门 checkpoint 节奏）；③保留中断权。
SKILL.md §0.5 卡片确认硬闸处挂钩。新增条款 **#53**（`问答到此结束`@00）。
指纹 52 → **53**。

## v2.1.7（2026-09-16）

**五大集成库自检升级为"问答+自检融合"**（用户需求：DSH 新项目未见集成库自检行为）。
根因：§0 步骤 2 的 `get_addon_status` 探测是**静默 API 调用**——条款在、执行无保证、
无用户可见输出。整改：
- §0 步骤 2 追加"**自检结果不得静默消费**"——五库开/关状态须随开档消息或档位选择项
  报给用户（含建议启用项），要用的库未开启时指引导勾选而非代勾；
- 00 分册问题池 A 型（来源优先）/B 型（资产来源）升级为问答+自检融合：问答前先探
  开关、报状态、再问需求。
新增条款 **#52**（`不得静默消费`@SKILL.md）。指纹 51 → **52**。

## v2.1.6（2026-09-16）

F-66 整改（DSH 侧 v2.1.5 审查发现，低危·元层）：13 §6⑥ 的 DETAIL_LEVEL 句未随
v2.1.5 修订同步（"修订未传导"第 4 次、传导纪律建立后首次）。取建议①：同步为
"降档不自动下调、**升级不得低于目标档下限**"并指名权威定义@00 第四维。
作者侧按传导纪律全包 grep DETAIL_LEVEL 十处逐一核对，确认唯此一处漏网；
其余近期修订术语（恒走/管线任务/档间规则全状态表）抽查无同类遗漏。
**规则语义层自洽 + 传导完整，本版起无已知缺口。**

## v2.1.5（2026-09-16）

F-65 整改（DSH 侧 v2.1.4 审查发现，低危）：第四维单向——只定义"降档不自动下调"，
未定义升级方向（升无限挡时 standard 低于新档下限 fine）。**同时采纳审计二维展开建议**：
- 第四维补双向：**升级不得低于目标档下限**（升无限挡须 ≥fine，或按卡片④预算裁并声明）
  ——"绑死最低保证"对升级方向同样适用；
- 补**时点维**（审计建议的 方向×情形 展开）：澄清阶段升降按新档配额/卡片演进；
  执行中升级 = 剩余工作按新档门配置与下限抬升（不追溯重做）；执行中降档 = 省略门、
  R4 留痕。
新增条款 **#51**（`不得低于目标档下限`@00）。指纹 50 → **51**。

## v2.1.4（2026-09-16）

F-64 整改（DSH 侧 v2.1.3 审查发现，低危）：全状态表第三维未成维——降档下限禁令仅
覆盖"建模任务"，非建模任务（材质/布光类，验收走 12 分册 checkpoint）降档下限未规定。
采纳建议①：禁令改**任务类型中立**——"管线任务不得降至直通档"（管线任务 = 13 分册
几何管线或 12 分册四阶段 checkpoint 任一）；表内格、表下说明、13 §6⑥ 三处同步。
**按审计四维建议一次列齐**：{档}×{方向}×{任务类型}×{DETAIL_LEVEL 联动}——第四维成文
（DETAIL_LEVEL 不随降档自动下调，用户可显式调低须声明）。新增条款 **#49/#50**。
指纹 48 → **50**。

## v2.1.3（2026-09-16）

F-63 整改（DSH 侧 v2.1.2 审查发现，低危）：降档梯度自称"逐级双向"，与升级跨档直达
明文不对称。**采纳审计过程建议，弃逐点修补改一次穷举收口**：00 降档通道改写为
**档间规则全状态表**（{直通,快速,完整,无限}×{升级,降档} 逐格定义），升级跨档直达与
降档逐级的不对称显式声明为有意设计（升级只是加问，降档才涉门配置）；13 §6⑥ 同步。
条款 #47 指纹随表更新（`档间规则全状态表`），新增 **#48**（`升级跨档直达，不受降档
逐级约束`）。指纹 47 → **48**。

## v2.1.2（2026-09-16）

F-62 整改（DSH 侧 v2.1.1 审查发现，低危）：降档梯度未涵盖最高档——修 F-60 时的
端点遗漏 → 梯度补全两端：**无限挡 ↔ 完整档 ↔ 快速档 逐级双向**，每降一档执行目标档
过程门配置；00 降档通道与 13 §6⑥ 两处同步。新增条款 **#47**（`逐级双向`@00）。
指纹 46 → **47**。

## v2.1.1（2026-09-16）

v2.1 验收三项低危整改（F-59~F-61，全部落在审计矩阵"边界"行——核心三轴已自洽）：
- **F-59**：条款 #44 指纹内嵌 markdown `**`（违反 F-36 语义指纹规范）→ 改语义串
  `阶段 5 拓扑清理`（件内唯一）；
- **F-60**：降档未定义目标档 → 梯度成文：完整档↔快速档双向，**建模任务不得降至直通档**
  （快速档赶时间 = 快速档 + draft + 产出物从简）；新增条款 **#46**；
- **F-61**：13 分册头部摘要与 §1 表对齐（"修订未传导"第三次出现）→
  **"修订后全局检索被引术语"写入作者侧修订纪律**（HANDOFF），系统性阻断。
指纹 45 → **46**。

## v2.1（2026-09-16）

**执行契约与质量门体系专项审计整改**（DSH 侧专项报告，F-55~F-58，3 中危 + 1 低危；
该专项补足了前三轮仅 diff 级审查的缺口——架构级自洽审计自此固化为架构变更的固定审计项）：
- **F-55**（中危）：DETAIL_LEVEL 停手位置（standard→阶段4）与 G3/G4（阶段5/6）不可共存
  → 重定义为**细节深度上限**，阶段 5 清理/阶段 6 交付**恒走**（draft 也需要干净拓扑与交付）；
- **F-56**（中危）：G1 位置不可变 vs 快速档默认合并 G1+G2，且合并时点无解 →
  **删"合并"概念**：快速档四门逐过（产出物从简）、§6⑥ 改降档执行、G1 不可省略；
- **F-57**（中危）：两层语义无层级表依据 → §1 表 Tier 1 显式纳入"各阶段出口判据"，
  Tier 2 缩窄为"阶段顺序与做法"；
- **F-58**（低危）：R1 补"作为最终渲染证据时"语境限定，过程层固化截图正当但不替代。
指纹 43 → **45**（#44/#45）。版本取 **minor** 依据：DETAIL_LEVEL 语义修正为行为可感知变化；
未达 major，因其为"使契约可一致执行"的修正而非契约变更。

## v2.0.3（2026-09-16）

F-54 整改（DSH 侧 v2.0.2 审查发现，低危·可选处置采纳）：CHANGELOG.md 作为 F-50
整改产物纳入"修复即配机器指纹"纪律 → 新增条款 **#43**（指纹
`major = 行为契约变更` @ 本文件版本语义定义行，件内唯一）。条款集边界同步裁定：
**关键行为条款 + 审计整改产物**纳入指纹；04/05/10/11 等知识分册与 docs/ 不在其列。
指纹 42 → 43。

## v2.0.2（2026-09-16）

F-53 整改（DSH 侧 v2.0.1 审查发现，低危）：F-49 的修复未配套回归条款，违反
"修复即配机器指纹"纪律 → 新增条款 **#41**（`永不静默省略`@SKILL.md）与 **#42**
（`不带理由视为静默省略`@00 分册），F-49 两个要点（永不静默省略 / N/A 须附理由）
分别锁定，指纹 40 → 42。

## v2.0.1（2026-09-16）

F-49~F-52 四项整改（DSH 侧 v2.0 审查发现）：
- **F-49**（中危）："底线永不豁免"与"不适用标 N/A"同句矛盾 → 全局改为"**永不静默省略**"，
  N/A 须附短理由（`R1 N/A` 三字裸标 = 静默省略 = 未完成）；直通档示例同步。
- **F-50**（低危）：大版本无立版说明 → 本文件建立（见下 v2.0/v1.10.6 条目）。
- **F-51**（低危）：条款 40 指纹错位（保护句与指纹句不同句）→ 指纹改 `必读 13 分册`。
- **F-52**（中危）：check-deploy 覆盖随调用副本变化（非 .zcode 副本无 local → 项目级根
  不受检却 PASS）→ local 查找加 **~/.zcode 主副本回退**；无 local 输出醒目 NOTICE；
  PASS 行标注机器专属根覆盖来源；新增 `deploy-roots.local.txt.example` 引导各机自建。

## v2.0（2026-09-15）

**立版说明（F-50 整改补记）**：1.x → 2.0 的依据是**行为契约变更**——
- 档位语义从"反问强度"架构级扩展为"**交互强度 × 出品契约**"（00 分册「执行契约与质量门」）；
- 交付宣告行成为强制格式，敷衍拦截四条生效（完成 = 宣告行逐项有证据）。
v2.0 相对 v1.10.6 仅含作者侧全量自审的 9 项修订（自审报告见工作区
《全量自审_v1.10.6_升v2.0.md》）；**质量门体系的实质内容（G/R 门、执行契约表、
敷衍拦截、回归指纹 #37~#40）由 v1.10.6 引入**——该版因与 v1.10.5 修复同日连发
而只进位补丁号，属版本策略失误，本表在此追认其为大版本级内容。

## v1.10.x（2026-09-15）

发布机制机器化三部曲：四查确立（v1.10.3 第④查）→ `check-deploy.py` 逐根哈希断言
（v1.10.4，F-46）→ 根集合断言 + `deploy-roots.txt` 入包（v1.10.5，F-47，首跑即抓出
第五根 .codebuddy）→ 内置/local 清单拆分（v1.10.6 前半，F-48）。

## v1.10.6（2026-09-15）

**四档质量门体系落地**（实质重大更新，见 v2.0 立版说明）：
执行契约表（必读册/DETAIL_LEVEL 下限/过程门 G0~G4/出品门 R1~R6）+ 敷衍拦截四条；
SKILL.md §0.5 挂钩"走建模管线的任务必读 13 分册"；指纹 36 → 40。
研究依据：LLM 自纠不可靠（Huang 2023 / 自我偏袒 NeurIPS 2024）→ 门全机验化；
外包 QA 门序列（门数随复杂度伸缩 = 档位原型）。

## v1.9.x ~ v1.10.2（2026-09-14 ~ 15）

回归指纹体系（26 → 36 条）、双 .cmd 零依赖启动器、遗留项台账、
发布前三查→四查、表头去数字化、台账同版登记纪律。

## v1.8 ~ v1.9（2026-09-13 ~ 14）

13 分册建模工艺（三级分层/七阶段管线/停手判据）、DETAIL_LEVEL 参数、
BMCP-ERR-014 烘焙方向、DSH Cordis 注册指南。

## v1.6 ~ v1.7（2026-09-13）

版本机器断言 check-version、跨根同名治理（移除 .agents 根）、
触发评测闭环（q6 修复，双平台 20/20）、evals 回归 runner。

## v1.0 ~ v1.5.1（2026-09-12）

初版至五轮审计闭环：13 册知识体系、四档 PRD 反问循环、安全模式实测结论、
隔离纪律（BMCP-ERR-001~007）、五层防御、回归指纹 26 条起步。
