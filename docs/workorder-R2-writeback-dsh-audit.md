# 工单：WO-R2 — DSH 安装实战审计回写（F-01~F-21，21+1 条发现）

| 项 | 值 |
|---|---|
| 工单号 | WO-BMCP-R2 |
| 日期 | 2026-09-12 |
| 状态 | **已执行完毕（v1.3 落地并经 DSH 二次审计逐条核实 21/21+1 全过，2026-09-12；V-03 勘误：本字段此前忘改）** |
| 上游 | `Blender技能审计_MCP安装实战_20260912\`（BMCP-INST-20260912，7 份报告 + raw/scripts/renders 全套留痕）；v1.2.1 包在 DSH 六宿主安装实战 + 独立 agent 交叉验证 + 参数化楼梯端到端（7/7 判据过） |
| 目标产物 | skill v1.3：P0 三处安全修正 + P1 六处文档修正 + P2 五处一致性修正 + P3 三处人因修正 |
| 红线 | 正向确认的 22 条断言原文不动；每条修正保留 F-xx 溯源号；SKILL.md ≤500 行 |

---

## 0. 审计结论接收摘要

- 装上了：6 宿主根 34/34 哈希复验，DSH 会话发现即生效（rank 100 项目级优先）；
- 不是升级：ZCode 侧本就是同一份 v1.2.1（逐字节一致），本次为再分发；
- 实战：参数化楼梯按自家协议端到端建成，**7/7 验收判据通过**，845 物体零漂移；
- 跨 agent：独立 agent（zai/glm-5.2，无共享上下文）3 次迭代跑通无头路线，路由表有效；
- 事故一次：**用户 AMS_World 被隔离窗口内的 orphans_purge 静默删除**（三条规则跨册合谋），
  靠 skill 自己的快照规则（铁律 7）完整恢复并逐名对账零差异——**铁律 7 的实战价值被硬验证**；
- 正负比：22 条正向确认（含 32 输入逐项一致、Safe Mode 十条禁令全中）vs 21+1 条发现
  （5 高危 / 6 中危 / 9 低危）。

## 1. 回写清单（按审计 §8 优先级，全部带 F-xx 溯源）

### P0（照文档写会直接失败 / 已致真实事故）
1. **F-01**：SKILL.md §0 与全部示例的 `get_scene_info` 补必填 `user_prompt`（新版服务端校验必需；
   该参数为遥测/轨迹用途，要求逐字用户原话，多步任务全程传同一目标）；
2. **F-10**：隔离期禁 purge 三处同改——03 §6.3 补"隔离期间禁止任何 orphans_purge，先还原
   用户 world/camera 再清理"；SKILL.md §5 清理步骤加前置条件；12 §7 登记表新增
   **BMCP-ERR-005**（隔离窗口 purge 删 users==0 的用户 World）；
3. **F-11**：12 §2 补"**阶段依赖单向**：重跑任一上游阶段后必须按序重跑下游全部阶段"
   （实测：只重跑几何 → 13 物体材质指派全部静默丢失，物体数/前缀/面数全达标不报错）；
   登记表新增 **BMCP-ERR-006**；骨架模板加"阶段依赖戳"（上游 report digest 写入物体
   自定义属性，下游启动时校验）。

### P1（版本漂移 / 协议陷阱 / 文档缺口）
4. **F-02**：INSTALL.md 版本口径改为"按协议 5 / addon v1.6 对齐"；显式声明服务端自报版本号
   可能为 mcp 依赖版本（serverInfo "1.30.0"，dist-info 实为 1.9.1——**本工单修正审计 F-02 的
   归因**：不是装了新版 blender-mcp，而是 FastMCP 把依赖版本写进了 serverInfo）；
   声明 1.30+ 自报版本的工具新增 `user_prompt`；
5. **F-03**：INSTALL.md 补遥测双开关：环境变量只关服务端日志侧；addon 侧 `telemetry_consent`
   默认 true，须调用 `disable_telemetry` 工具（一次性、只能关）或在 Blender 偏好取消勾选；
6. **F-04**：09 分册"错误回传三形态"表加"协议层 isError 恒为 false"列；判据=以 result 文本
   前缀为准（`Code executed successfully:` / `Rejected by safe mode` / `Error executing code:`）；
7. **F-06**：03 分册 GPU 段加警示"受 Safe Mode 限制不可执行（preferences 链式导航被拒）；
   需按 09 §4B 独立子进程通道"；
8. **F-05**：09 分册（或 11）补"**AST 校验是整脚本闸门**：一处违禁=整个脚本零执行（含前面
   合法语句）"——与铁律 3"报错前语句已生效"方向相反，两者必须并列写清；
9. **F-21**：INSTALL.md 宿主表补 DSH 两行（`<项目>/.dsh/skills` rank100 + `~/.dsh/skills`
   rank400；**用户级安装会撞 workspace-write 沙箱**，项目级无门槛；发现即生效无需重启）。

### P2（内部矛盾 / 自家示例违反自家铁律）
10. **F-07**：12 分册禁令列表删除 `do_unlink`（实测 5.2.1 反复可用，01/08 分册示例在用）；
11. **F-09**：02 分册两处裸 `next()` 改 `next((...), None)` + 判断（02 违反自家铁律 5）；
12. **F-08**：铁律 1 补"计时只能 MCP 客户端侧做（白名单禁 `import time`），agent 应记录
    每次调用墙钟耗时"；
13. **F-12**：SKILL.md §5 补前缀分层约定 `TUT_<阶段>_<语义>`（TUT_GEO_*/TUT_RIG_*），
    `reset_prefix` 传最具体前缀（否则几何阶段重跑会删掉灯光相机装置）；
14. **F-13**：SKILL.md §8 展开为完整可复制脚本（或显式标注"执行需先读 01+03 分册"——
    独立 agent RUN1 崩在"设活动集合无 API 写法"，RUN2 崩在回读顺序）；
15. **F-18**：`scene_audit.py` 输出补 `skipped_hidden` 计数（隔离窗口内 hide_render 物体
    被静默跳过会造成"报告正常却漏 845 物体"的盲区）。

### P3（人因 / 体验）
16. **F-20**：SKILL.md §3 路由表每行加 markdown 链接（全包目前零可点击链接，人因复查成本高）；
17. **F-19**：nodes/ 补首个实体节点组脚本（或 §8 阶梯标注"当前为空落下一级"）；
18. **F-16/F-17**：10 分册"无头 EEVEE 黑图"拆两档（有 GPU 桌面机可用——实测 6.5~33.7s 成功 /
    真·无 GPU CI 才黑图）；03 §3【修3】补经验判据"三角面 <5 万且 ≤720p/64spp 时 CPU
    Cycles 通常 <30s（实测 3.96s），以首次冒烟实测耗时为准记入 report"；
19. **F-14**：09 分册新增"**StructRNA 生命周期**"条目（remove 后访问属性 → ReferenceError；
    先取值后删除；审计者自己也踩了 A-4，三类人群同踩=必须立规）；
20. **F-15**：03 分册 world 示例去掉 `use_nodes` 赋值（5.2 抛 DeprecationWarning）。

## 2. 明确不做
- 正向确认 22 条对应的原文一律不改（含 32 输入名单/禁令表/引擎 ID 等）；
- DSH 侧建议（D-1 沙箱、D-2 MCP 接线）不属 skill 范围，转交用户在 DSH 侧处置
  （接线包在审计目录 `接线到DSH/`，未执行）；
- 楼梯实战与坦克建模审计目录的产物不回写。

## 3. 验收标准（Given/When/Then）
1. 新会话冷启动首调 `get_scene_info(user_prompt=...)` 成功（F-01 修复验证）；
2. Given 渲染隔离窗口 When 跑 orphans_purge Then 00/03/SKILL/12 四处条款都拦截该顺序；
3. Given 只重跑几何阶段 When 探针 Then 12 分册条款要求重跑下游且骨架校验 material_slots>0；
4. 09 分册三形态表含 isError 列与文本前缀判据；
5. INSTALL.md 宿主表含 DSH 两行 + 遥测双开关 + 版本口径修正；
6. 02/12 分册无裸 next()/无 do_unlink 禁令矛盾；scene_audit 输出含 skipped_hidden；
7. SKILL.md ≤500 行；22 条正向确认对应的原文零 diff。

## 4. 执行顺序
P0（1→2→3）→ P1（4~9）→ P2（10~15）→ P3（16~20）→ 验收 → 记忆更新（v1.3 +
BMCP-ERR-005/006）。纯本地，无子代理。预估 ~40 分钟。

---
（本工单依据 DSH 审计 06 号报告 §8 回写建议整理；审计原文与全部 raw 证据在其目录内。）

## 执行回执（2026-09-12 追加，V-03）

- 执行结果：P0/P1/P2/P3 二十项全部落地（grep 终检全过 + 骨架带戳实跑 + WD_wood 建组/实例化实测）；
- DSH 二次审计逐条核实 21/21+1 全过（见 `Blender技能审计_MCP_v1.3_20260912\01`）；
- 审计对本单的唯一批评：执行后本状态字段忘改（本段即 V-03 勘误）。
