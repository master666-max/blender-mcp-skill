# 工单：blender-mcp skill v1.1 合并执行单（001+002+003 → R1）

| 项 | 值 |
|---|---|
| 工单号 | WO-BMCP-R1（合并版） |
| 日期 | 2026-09-12 |
| 状态 | **待执行（全部拍板完毕：12 项裁定齐备；本单为唯一执行依据）** |
| 上游单据 | WO-001（PRD 反问循环，7 裁定）、WO-002（实战发现吸收，无待拍板项、设计即定稿）、WO-003（力大砖飞架构吸收，4 裁定）——原单保留作设计与裁定记录，**执行细节以本单为准** |
| 目标产物 | blender-mcp skill **v1.1**：PRD 反问循环 + 部署安全分册 + 力大砖飞执行契约 + 离线安装包捆绑 + 组件库 |
| 红线 | 用户场景零污染；全部新代码过安全模式白名单；01~08/10 分册实测结论零 diff（09 只增不改）；SKILL.md ≤500 行；子代理并发=1（仅 P0 调研占用，串行一路） |

---

## 1. 合并效力说明

- 001 裁定 7 项（先连后问/语言跟随/卡片落盘/完整档保留/纯对话模板/无限循环挡/安装包捆绑）、
  003 裁定 4 项（骨架全强制/组件库 skill 包内/M12 入契约/与 001 合并）**全部继承**；
- 002 无开放问题（原文即定稿设计），随合并直接生效；
- 三单共同指向的 SKILL.md 改动改为**单次统合修改**（P5），避免三次编辑同一文件。

## 2. 统一改动清单（文件 × 改动 × 来源）

| 文件 | 改动 | 来源 |
|---|---|---|
| `references/00-prd-intake.md` | 新增 ~130 行：触发门槛（直通/快速/完整/无限循环挡四档）、分型问题池（7 型×6-10 问+默认值）、PRD 卡片模板（七节+可机验验收+假设区）、示例对话 2 则、问题语言规则 | 001 |
| `references/11-deployment-security.md` | 新增 ~130 行：安装部署（uv tool/config.json/env 四变量）、威胁模型五通道、五层防御、维护速查；来源标注隔壁会话文档+本会话调研 | 002 |
| `references/12-brickfly-protocol.md` | 新增 ~150 行：M1~M12 执行契约（骨架五段式/四阶段隔离/三级反馈/串行多路径/教训编号/复用阶梯/映射表），携带六条平台修正 | 003 |
| `references/09` | 增补两条：僵尸 blender-mcp.exe 故障模式（netstat 判定法）、Safe Mode 通道真相（高级模式条目+警示） | 002 |
| `scripts/brickfly_skeleton.py` | 新增：五段式骨架 + set_input 候选名 helper + find_node 中文 UI 安全查找 + report 组装；头部注明适用范围（写操作全骨架/只读豁免） | 003 |
| `fragments/` + `nodes/` | 新目录+README×2（积木格式契约/nodes 仅 .py 重建脚本约束）+ 首两块积木（three_point_rig / mesh_audit，自三件套抽象） | 003 |
| `assets/blender-mcp-bundle/` | addon.py + PyPI wheel（锁定版本）+ ATTRIBUTION.md（作者 Siddharth Ahuja @sidahuj + 许可）+ SHA256SUMS.txt | 001-裁定⑦ |
| `evals/eval-queries.json` | +5 条（"做个椅子"应触发反问；"面数多少"不应触发等） | 001 |
| `SKILL.md` | **单次统合**（见 §4，合计增量 ≤60 行） | 三单 |
| `docs/architecture-brickfly-v3.md` | 收录架构 v3.0 原文，标注"设计上游" | 003 |
| frontmatter description | 更新：加入"先澄清再动手/需求反问"触发语义 → 重跑触发评测 | 001+003 |

## 3. 执行阶段（串行总序）

| 阶段 | 内容 | 资源 |
|---|---|---|
| **P0 调研** | 001 §2 的 R2（PRD 方法论）→ R3（GitHub：PRD 模板/澄清循环 agent/**3D 委托单 brief**）→ R4（社区委托帖字段池），产出喂给 P4 的问题池定稿 | 子代理**串行一路**，唯一并发占用 |
| **P1 捆绑安装包** | 001 §3.5 五步：本机只读提取 addon.py → 官方源下载锁定版本 wheel → sha256 三方比对（不一致停工上报）→ LICENSE 再分发核查（禁则降级为命令记录）→ 落 assets/ + 离线安装小节素材 | 本地+网络 |
| **P2 部署安全** | 写 11 分册；09 增补两条 | 本地 |
| **P3 架构契约** | 收录架构原文 → brickfly_skeleton.py → fragments/nodes → 12 分册（六条平台修正逐条带【实测】来源） | 本地 |
| **P4 PRD 分册** | 00 分册（P0 结果定稿问题池与默认值）+ evals +5 | 本地 |
| **P5 SKILL.md 统合** | 单次修改：§0.5 需求澄清（四档+PRD 卡片流）＋ §1 铁律 2 补通道真相一句 ＋ §3 路由表 +2 行（00/11/12）＋ §6 恢复 SOP 加"第 0 步查僵尸" ＋ §7 表后加 11 分册指针 ＋ §9 后加三行协议摘要 ＋ description 更新 | 本地 |
| **P6 统一验收** | §5 清单逐条 + 触发评测重跑 + 验收报告待复核 + 记忆更新（v1.1） | 本地+MCP |

## 4. SKILL.md 统合改动明细（P5 一次过）

1. 新增 **§0.5 需求澄清（PRD 反问循环）**：连接自检之后、路由表之前；四档触发+卡片流+
   无限挡机制，正文 ≤25 行，细节指 00 分册；
2. §1 铁律 2 末尾补一句："Safe Mode 拦的是本通道；其他进程可自带 SAFE_MODE=0 另起连接
   （09/11 分册）"；
3. §3 路由表 +2 行：需求澄清/反问循环 → 00；部署/安全/维护 → 11；系统化建模/多阶段 → 12
   （实为 +3 行，压缩进表尾）；
4. §6 恢复 SOP 加第 0 步：查僵尸 `netstat -ano | findstr 9876`；
5. §7 资产表后加一行：部署与安全 → 11 分册；离线安装包在 assets/；
6. §9 之后加三行"建模任务协议（力大砖飞）摘要"：写操作代码一律五段式骨架；多阶段任务
   几何→材质→构图→灯光隔离推进；看图三级分级——细节指 12 分册；
7. description 增补触发语义（"先澄清再动手""需求不明确会反问"）。

## 5. 统一验收清单（P6 逐条勾）

1. 11 分册四节齐全（部署/威胁模型/五层防御/维护速查），每节带来源与【实测】/【文献】分级；
2. 09 新两条可用：netstat 判定法在本机可执行出结论；条目能回答"为什么有弹窗"与
   "'AI 不能关 Safe Mode'哪句话不严谨"两问；
3. 00 分册：四档齐备（含无限循环挡机制）、7 型问题池、PRD 卡片模板、示例对话 2 则；
4. 捆绑包：addon.py 与 wheel 的 sha256 与官方源一致并入册；ATTRIBUTION 含作者署名与许可；
   SKILL.md 有离线安装小节；本机已装插件只读未触碰；许可禁再分发则降级为命令记录（同样算过）；
5. skeleton 幂等实跑：连跑 2 次物体数一致，report JSON 可解析、无中文字符裸输出；
6. fragments 两块积木实跑达标（布光回执 / 审计 JSON）；
7. 12 分册：六条平台修正全部在册且标注【实测】来源；M1~M12 契约条款齐备；
8. 12 分册含卡片→参数区映射表，与 00 分册互引一致（grep 核对）；
9. 端到端实测：模拟两阶段任务（立方体+材质+快渲）按 12 协议走通，全程无超时重试，
   每阶段有 report 与截图判据；
10. 无限循环挡行为实测：连续 3 轮追加需求→每轮新问题+卡片演进；说"开工"→零追问退出；
11. SKILL.md ≤500 行；01~08/10 分册零 diff（09 仅增补）；
12. description 更新后触发评测全集通过率 ≥ 原基线（15 旧+5 新）；
13. 场景零污染对账：验收用例清理后物体数回到任务前值。

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| P0 调研超时/限流 | 串行一路本就是兜底形态；单路失败重试一次后跳过，问题池以本地素材（隔壁 brief 惯例+001 内置字段池）兜底定稿 |
| SKILL.md 一次改动多、核对负担大 | §4 明细逐条对照 + 验收 11 行数关卡；改动分小节提交（不 commit，除非用户要求） |
| 捆绑许可核查不利（禁再分发） | 001-裁定⑦ 已预设降级路径：只记获取命令，不落文件，验收 4 有替代通过分支 |
| 三分册互引漂移 | 验收 8 grep 核对 + 12 分册文件头写"上游=架构 docs，下游=00 互引" |

## 7. 裁定记录（2026-09-12，用户裁定：description 按"改"执行）

| # | 议题 | 裁定 | 一句话理由 |
|---|---|---|---|
| ⑫ | description 触发语义 | **改**：frontmatter description 加入"需求不明确时先反问澄清（PRD）"触发语义 | 新行为必须可被触发才有意义；改后 P6 重跑 20 条评测兜底防触发回归 |

至此本单裁定全部齐备（继承 001 七项 + 003 四项 + 本项 = 12 项），无剩余待拍板。

## 8. 执行后动作

- skill frontmatter `metadata.version` → "1.1"；
- 用户记忆库 blender-mcp-skill-project.md 追加 v1.1 变更行；
- 三张原单文件头加"已由 R1 执行完毕"状态注记（保留作裁定档案）。

---
（合并单本身不含新裁定，仅统合既有 11 项裁定与三份设计；未执行任何改动。）
